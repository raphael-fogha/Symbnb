{% extends 'base.html.twig' %}

{% block title %}
    Bienvenue sur notre réseau d'appartement !
{% endblock %}

{% block header %}
    <h2>Voci le header</h2>
{% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron mt-3">
        <h1 class="display-3">Bienvenue sur SymBNB !</h1>
        <p class="lead">Nous mettons en relation les voyageurs et le propriétaires ! <br>
        <small>(Absolument pas inspiré d'un certain site déjà trés connu dans le secteur)</small></p>
        <hr class="my-4">
        <p>Vous êtes voyageur ? Et vous voulez voir ou vous pourrez vous logez pendant votre prochain sejour ?</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="{{ path('ads_index') }}" role="button">Voir nos annonces</a>
        </p>
    </div>

    <h2 class="mt-3 mb-2">Nos logements stars !</h2>

    <div class="row ">
    	{% for ad in pagination.data %}
				{% include 'ad/_ad.html.twig' %}
			{% endfor %}    
    </div>

    <h2 class="mt-3 mb-2">Nos propriétaires stars</h2>

    <div class="row">
        <div class="col">
            <div class="card border-secondary mb-3">
                <div class="card-body">
                    <h4 class="card-title">{{user1.fullName}}</h4>

                    <hr>

                    <div class="card-text">
                        <img src="{{user1.picture}}" alt="avatar" class="avatar avatar-medium float-left mr-3">
                        <p>{{user1.description|striptags|slice(0,100)}}
                        </p>
                        <a href="{{path("user_show", {'slug' : user1.slug})}}" class="btn btn-success float-right">En savoir plus</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card border-secondary mb-3">
                <div class="card-body">
                    <h4 class="card-title">{{user2.fullName}}</h4>

                    <hr>

                    <div class="card-text">
                        <img src="{{user2.picture}}" alt="avatar" class="avatar avatar-medium  float-left mr-3">
                        <p>{{user2.description|striptags|slice(0,100)}}
                        </p>
                        <a href="{{path("user_show", {'slug' : user2.slug})}}" class="btn btn-success float-right">En savoir plus</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>    
{% endblock %}
